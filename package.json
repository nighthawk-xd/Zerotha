{
  "name": "zerodha-clone-workspace",
  "version": "1.0.0",
  "description": "Zerodha clone monorepo",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix dashboard",
    "build:frontend": "npm run build --prefix frontend",
    "build:dashboard": "npm run build --prefix dashboard",
    "build:all": "npm run build:frontend && npm run build:dashboard",
    "start": "npm start --prefix backend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\" \"npm start --prefix dashboard\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
